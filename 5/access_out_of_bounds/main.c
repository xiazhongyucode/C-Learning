#include <stdio.h>

//编译器不检查程序对数组下标的引用是否在数组的合法范围内
int main() {
    int a[5] = {1, 2, 3, 4, 5};//定义数组时，数组长度必须固定
    int j = 20;
    int i = 10;
    a[5] = 6;//访问越界
    a[6] = 7;//越界访问会造成数据异常
    printf("i = %d\n", i);//i发生改变
    return 0;
}
